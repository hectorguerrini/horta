import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:horta/app/modules/hortasLista/hortas_lista_page.dart';

void main() {
  testWidgets('HortasListaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(HortasListaPage(title: 'HortasLista')));
    //  final titleFinder = find.text('HortasLista');
    //  expect(titleFinder, findsOneWidget);
  });
}
