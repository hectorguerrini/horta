import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:horta/app/modules/clientes/clientes_controller.dart';
import 'package:horta/app/modules/clientes/clientes_module.dart';

void main() {
  initModule(ClientesModule());
  // ClientesController clientes;
  //
  setUp(() {
    //     clientes = ClientesModule.to.get<ClientesController>();
  });

  group('ClientesController Test', () {
    //   test("First Test", () {
    //     expect(clientes, isInstanceOf<ClientesController>());
    //   });

    //   test("Set Value", () {
    //     expect(clientes.value, equals(0));
    //     clientes.increment();
    //     expect(clientes.value, equals(1));
    //   });
  });
}
