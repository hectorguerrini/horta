import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:horta/app/modules/agricultor/agricultor_page.dart';

void main() {
  testWidgets('AgricultorPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AgricultorPage(title: 'Agricultor')));
    //  final titleFinder = find.text('Agricultor');
    //  expect(titleFinder, findsOneWidget);
  });
}
