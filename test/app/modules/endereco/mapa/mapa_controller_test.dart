import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:horta/app/modules/endereco/mapa/mapa_controller.dart';
import 'package:horta/app/modules/endereco/endereco_module.dart';

void main() {
  initModule(EnderecoModule());
  // MapaController mapa;
  //
  setUp(() {
    //     mapa = EnderecoModule.to.get<MapaController>();
  });

  group('MapaController Test', () {
    //   test("First Test", () {
    //     expect(mapa, isInstanceOf<MapaController>());
    //   });

    //   test("Set Value", () {
    //     expect(mapa.value, equals(0));
    //     mapa.increment();
    //     expect(mapa.value, equals(1));
    //   });
  });
}
