import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:horta/app/modules/endereco/endereco_page.dart';

void main() {
  testWidgets('EnderecoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EnderecoPage(title: 'Endereco')));
    //  final titleFinder = find.text('Endereco');
    //  expect(titleFinder, findsOneWidget);
  });
}
